================================================================================
SNOOZE LAW VERIFICATION (Contract section 48 — Snooze FULLY DEFERRED)
================================================================================

REQUIREMENT
  Snooze is deferred from the current product. Dormant legacy support may ONLY
  terminate or no-op. No Snooze UI, no Snooze action, no Snooze runtime.

PART 1 — NOTIFICATION ACTIONS (the decisive check)
  File: lib/core/notifications/flutter_local_notifications_gateway.dart
  The reminder notification details builder is ACTION-FREE. There are no
  androidAction entries and no snooze action button anywhere in the reminder
  notification construction path.

  The file also carries the explicit owner-decision comment recording that the
  notification body's own tap is the canonical Open path, using the same
  payload/routing as the retired button — i.e. the Open path survives while the
  action buttons do not.

  Scans over all modified production files for these terms returned NO
  occurrences:
      snoozeAction        (no occurrence)
      SnoozeAction        (no occurrence)
      ACTION_OPEN         (no occurrence)
      openAction          (no occurrence)
      notificationActionButton (no occurrence)
  See 05_FORBIDDEN_FEATURE_SCAN.txt in this directory.

PART 2 — RESIDUAL SNOOZE REFERENCES ARE DATA-ONLY
  Two residual references exist. Both were inspected individually:

  (a) lib/core/background/reminder_recovery_request.dart
      Occurrences: 2 — both mere field handling of the PRE-EXISTING
       column on the background_work_requests table:
          final snoozeCount = existing?.snoozeCount ?? 0;   // read
          snoozeCount: Value(snoozeCount),                  // preserve on write
      This is idempotency preservation: the marker writer must not clobber an
      inherited column. No Snooze behaviour is added, invoked, or exposed.

  (b) lib/features/notifications/application/reminder_background_runtime.dart
      Occurrence: 1 — `snoozeIntent: snooze,` passed through an existing
      delivery call signature. This is dormant legacy parameter plumbing on an
      existing call path, not a new Snooze capability. It cannot be reached by
      the user because no notification action and no UI surfaces it.

PART 3 — NEW FILES
  Of the 9 new production files, 8 contain ZERO occurrences of 'snooze'.
  The single file with occurrences is (a) above, accounted for.

PART 4 — SCHEMA
  No new snooze column, table, or migration was introduced. The schema remains
  v46 and the database layer is unchanged (see 01_SCHEMA_V46_EVIDENCE.txt).
  The `snoozeCount` column cited above is PRE-EXISTING in the baseline.

PART 5 — SCHEDULE MODE PRESERVED
  lib/core/notifications/flutter_local_notifications_gateway.dart:91
      androidScheduleMode: AndroidScheduleMode.inexactAllowWhileIdle,
  The inexact schedule mode is UNCHANGED, i.e. no exact-alarm behaviour was
  introduced. This is corroborated by the absence of any exact-alarm permission
  in the merged manifest (see 04_PERMISSION_DIFF_EVIDENCE.txt).
