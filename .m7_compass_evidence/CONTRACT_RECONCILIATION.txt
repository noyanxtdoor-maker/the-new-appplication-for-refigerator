================================================================================
VS16 — ASTRA M7/M8 CONTRACT RECONCILIATION RECORD
Compass function restoration (owner-authorized extension of Maps M7)
2026-09-13, worktree C:\Users\sherl\Downloads\NT_B5_DEVELOP__deepseek-m7m8-20260911
================================================================================

Authority: NEXT_TRANSFER_ASTRA_VS16_M7_M8_IMPLEMENTATION_CONTRACT_20260910.txt
plus the two supporting audits (current-baseline forensic, notifications/background
focused audit). Contract section 1 requires that subsequent explicit owner
instructions amending this contract be RECORDED. This file is that record.

--------------------------------------------------------------------------------
1. OWNER AMENDMENT RECORDED
--------------------------------------------------------------------------------
The owner's instruction of 2026-09-13 authorized a Maps-only restoration: the
visible top-left compass needle's tap → north-up function. That instruction is
narrower than the contract's M7 (notifications/contact-follow-up enrichment) and
is an explicit owner amendment to scope, not an implementer deviation. Its own
gates were stated by the owner: focused compass + maps camera/session tests only,
flutter analyze --no-pub --fatal-infos lib test, git diff --check, one fresh APK,
adb install -r, physical needle function proof, and an explicit short report
format. Those owner gates OVERRIDE the contract's broader section 51/58 gates for
this extension (the owner prohibited the 290-file global suite and the full
differential).

Naming collision to avoid in future records: "VS16 M7" in this worktree labels
BOTH (a) the Maps M7 line (this compass work) and (b) the contract's M7
notifications/Contact-purpose milestone. They are different workstreams.

--------------------------------------------------------------------------------
2. BASELINE / BRANCH VERIFICATION (read-only)
--------------------------------------------------------------------------------
HEAD                       : 7b1395c10232b51f7d59464214ffe915415c4bb1
                             == contract certified baseline, unchanged.
origin/main (git ls-remote): 7b1395c10232b51f7d59464214ffe915415c4bb1
                             == unchanged, so contract section 3 does not block.
branch (current)           : develop/vs16-m7-m8-deepseek-20260911
contract section 4 asks for: develop/vs16-m7-m8-20260910
                             -> DIVERGENCE RECORDED. No branch was created,
                             renamed, switched, reset or forced by this work.
inherited dirty state      : 96 status entries at the start of this work and the
                             same set afterwards apart from the files listed in
                             section 4 below. Nothing was reset, cleaned,
                             stashed, restored, committed or pushed.
tooling note               : contract section 3 forbids using
                             C:\Users\sherl\Documents\Next Transfer-Temp for
                             implementation. COMPLIED: that tree is not the
                             authoritative checkout and received only temporary
                             scratch tooling (screenshots/scripts), which was
                             removed at the end of this work. All edits were made
                             under the Downloads worktree above.

--------------------------------------------------------------------------------
3. SCHEMA DIVERGENCE RECORDED (not caused by this work)
--------------------------------------------------------------------------------
contract section 46: "SCHEMA CHANGE REQUIRED: NO. Keep version46."
working tree        : lib/core/database/app_database.dart already carried an
                      uncommitted 46 -> 47 change (inherited from the earlier M7
                      notification work in this dirty tree, +112/-2 lines,
                      documented as the v46->v47 onUpgrade / native-options fix).
                      The installed device DB reports user_version 47.

This work did NOT touch app_database.dart, app_database.g.dart, pubspec.yaml,
pubspec.lock, theme/font/asset files, manifests/signing/package/SDK settings,
Maps persistence or any generated file. Files changed by this work:

  android/maps_interaction_patch/java/io/flutter/plugins/googlemaps/
    NtPrecisionMapView.java  (+50/-3)  docked-SDK-control tap bypass
  android/maps_interaction_patch/README.md (+10)  documents that bypass
  test/features/maps/vs15_m33_auto_face_north_audit_test.dart (untracked M7 file)
                             +2 tests: compass-reset acceptance/persistence (11)
                             and the native control-press contract (12)
  .m7_compass_evidence/      evidence pack (this file, EVIDENCE.txt, 4 device
                             screenshots, verify.py harness)

Protected surfaces named by contract sections 50/53 were left untouched; the
change is confined to the project-owned native tap layer and its README, so the
Maps persistence / Saved Places / Google-Maps / MapLibre integration boundaries
are unchanged. Permission set unchanged.

--------------------------------------------------------------------------------
4. GATES: CONTRACT vs THIS EXTENSION
--------------------------------------------------------------------------------
run and PASSING here          : focused Maps tests (24 tests incl. new 11/12),
                                flutter analyze --no-pub --fatal-infos lib test
                                ("No issues found"), git diff --check exit 0,
                                one fresh final debug APK recorded with bytes +
                                SHA-256, data-preserving adb install -r, device
                                schema 47 / integrity ok / owner rows preserved,
                                physical needle function proof on the handset.
not run here (owner override) : section 51 fresh named baseline differential on
                                all suites; section 58 full implementation-report
                                format; M8 engineering items (none started).
transparency                  : three debug APKs were produced during this
                                extension (two diagnostic builds that carried a
                                temporary camera/bearing probe and a native
                                decision log, and the final clean build). The
                                probe and diagnostic code are absent from the
                                delivered APK (dex + kernel marker scan). Only the
                                final artifact is offered for acceptance:
                                build/app/outputs/flutter-apk/app-debug.apk
                                240,980,609 bytes
                                sha256 d7c9b6b807120f7296c328ab5b86f7cc82ed72fb4296b6247b2203451bbbf539

--------------------------------------------------------------------------------
5. STATUS OF THE CONTRACT'S OWN MILESTONES (unchanged by this record)
--------------------------------------------------------------------------------
M7 notifications/Contact-purpose work in this dirty tree: as previously
certified by its own M7 reports; not re-audited here.
M8: NOT STARTED (no contract M8 item was touched).
Commit / push / tag / PR: NOT PERFORMED. Main protection untouched.
================================================================================
